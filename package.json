{
  "name": "dskd",
  "description": "ウェブ制作にまつわる雑記を主としたウェブログです。2010年12月より公開。",
  "version": "8.12.0",
  "license": "CC BY-NC",
  "author": "oti",
  "keywords": [],
  "scripts": {
    "build": "rm -rf dist && node task/build.mjs",
    "markuplint": "markuplint dist/**/*.html",
    "prepare": "husky install",
    "start": "npm run build && cd dist && python3 -m http.server 3000",
    "stylelint": "stylelint dist/**/*.css",
    "test": "npm run stylelint && npm run markuplint"
  },
  "devDependencies": {
    "fast-glob": "^3.2.12",
    "gray-matter": "^4.0.3",
    "husky": "^8.0.3",
    "markdown-to-pug": "^1.0.11",
    "markuplint": "^3.4.0",
    "pug": "^3.0.2",
    "stylelint": "^15.2.0",
    "stylelint-config-recommended": "^10.0.1"
  }
}
