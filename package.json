{
  "name": "dskd",
  "version": "9.7.0",
  "description": "越智による個人ウェブサイト『dskd』の開発環境",
  "keywords": [],
  "license": "CC BY-NC",
  "author": "oti",
  "scripts": {
    "build": "rm -rf dist && node task/build.mjs && npx prettier --write dist",
    "create-draft-post": "node task/draft-post.mjs",
    "markuplint": "markuplint 'dist/**/*.html'",
    "prepare": "husky install",
    "start": "npm run build && cd dist && python3 -m http.server 3000",
    "stylelint": "stylelint 'dist/**/*.css'",
    "test": "npm run stylelint && npm run markuplint"
  },
  "devDependencies": {
    "fast-glob": "^3.3.1",
    "gray-matter": "^4.0.3",
    "husky": "^8.0.3",
    "marked": "^8.0.0",
    "markuplint": "^3.12.0",
    "pug": "^3.0.2",
    "stylelint": "^15.10.3"
  }
}
