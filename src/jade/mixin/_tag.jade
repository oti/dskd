//- _tag.jade
mixin tag(page_tag, page_type)
  p.post-tag
    - var last_tag = page_tag.length-1;
    each tag, i in page_tag
      if page_type === 'post'
        a.tag-link(href=('/'+archives_base+tag.toLowerCase().replace(' ', '_').replace('.', '_')+'.html'))=tag
        //- 最後以外は「, 」をつける
        if i < last_tag
          | , 
      else
        | #{tag}
        if i < last_tag
          | , 
